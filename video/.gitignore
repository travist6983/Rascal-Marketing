node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Props handed to `remotion render` by scripts/social-reel.mjs. Removed on a
# successful render; left behind after a failed one so it can be re-run.
.reel-props.json
